#!/bin/bash

set -eo pipefail

if [[ -z "$1" ]]; then
  echo "Please provide a ruby version!"
  exit 1
fi

set -u

if command -v brew; then
  time ruby-build --with-libyaml-dir=`brew --prefix libyaml` -v $1 ~/.rubies/$1
else
  time ruby-build -v $1 ~/.rubies/$1
fi
