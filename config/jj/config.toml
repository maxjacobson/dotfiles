[user]
name = "Max Jacobson"
email = "max@hardscrabble.net"

# Use work email for repos under FAMLI directory
[[--scope]]
--when.repositories = ["~/FAMLI"]
[--scope.user]
email = "maxwell.jacobson@maryland.gov"

[signing]
behavior = "own"
backend = "ssh"
key = "~/.ssh/id_ed25519"

[ui]
default-command = "log"
editor = "micro"

# Show author name instead of email in log
# (Commenting out for now while I do the tutorial at https://jj-for-everyone.github.io/ which expects a more stock setup for its examples)
# [template-aliases]
# 'format_short_signature(signature)' = 'author.name()'
