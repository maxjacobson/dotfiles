alias 't'='tmux new-session -A -s $(basename $PWD | tr -d .)'
alias 'tl'='tmux ls'

function ta {
  # if no argument was supplied, just attach to some session
  if [[ -z $1 ]]; then
    tmux attach

  # if an argument was supplied, attach to a specific session
  else
    tmux attach -t "$1"
  fi
}
