#!/bin/sh

set -e

git branch --sort=authordate --format='%(refname:short)' |
  grep -v "$(git branch --show-current)" |
  xargs gum filter --no-limit --no-fuzzy --reverse --header "Select branches to delete" |
  xargs git branch --delete --force
